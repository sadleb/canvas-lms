<%= t "BV %{body}", :body => CanvasTextHelper.truncate_text(html_to_text(asset.message), :max_length => 110) %>

<%= t :details, "%{url}", :url => BitlyHelper.new.bitly_shorten("#{HostUrl.protocol}://#{HostUrl.context_host(asset.context)}/courses/#{asset.context.id}/discussion_topics/#{asset.id}") %>
